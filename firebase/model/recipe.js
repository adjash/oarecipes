export default class Recipe {
    constructor(name, timings, ingredients, steps){
        this.name = name;
        this.timings = timings;
        this.ingredients = ingredients;
        this.steps = steps
    }
}